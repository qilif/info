#include <iostream>

using namespace std;

int main()
{
    int n,m,a,b,xx,j;
    cin>>k>>x>>a>>b;
    xx=x;
    for(int i=1; i<=k; i++)
        s+=((i*(i+1))/2);
    cout<<s%10<<'\n';
    for(j=0;xx;j++)
        xx/=10;
    if(x%10==j)
    return 0;
}
