#include <iostream>

using namespace std;

int v[500001], u[500001], w[101];
int main()
{
    int n, m, t, a, b, c, i, y, j;

    cin >> n;
    for(i = 1; i <= n; i++)
        cin >> v[i];
    i=1;
    cin >> m;
    for (j = 1; j <= m; j++)
        cin >> u[j];
    j=1;
    cin >> t;
    for (y=1; y<=t; y++)
        cin >> w[y];
    y=1;
    while (v[i] + u[j]!=w[y])
    {
        if (v)
    }
    return 0;
}
