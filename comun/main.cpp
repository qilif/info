#include <iostream>
#include <algorithm>

using namespace std;

int v[10000001];

int main()
{
    int n,m,i=1;
    cin>>n;
    for(int i=1;i<=n;i++)
        cin>>x[i];
    for(int i=1;i<=n;i++)
        cin>>y[i];
    for(int i=1;i<=n;i++)
        cin>>z[i];
    for(int i=1;i<=n;i++)
        if(x[i]==y[i]&&y[i]==z[i])
    return 0;
}
